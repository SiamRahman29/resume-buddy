{
  "mcpServers": {
    "latex-server": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}/vendor/mcp-latex-server",
        "${CLAUDE_PLUGIN_ROOT}/vendor/mcp-latex-server/latex_server.py"
      ],
      "env": {
        "LATEX_SERVER_BASE_PATH": "."
      }
    }
  }
}
