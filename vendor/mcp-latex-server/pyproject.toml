[project]
name = "mcp-latex-server"
version = "2.0.0"
description = "MCP server for LaTeX file creation, editing, validation, and compilation"
requires-python = ">=3.11.9"
dependencies = [
    "mcp[cli]>=1.9.4",
    "pydantic>=2.0.0",
]

[project.scripts]
mcp-latex-server = "latex_server:mcp.run"
