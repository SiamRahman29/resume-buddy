# Local dev-test artifacts (testing the plugin from inside this repo)
build/
/resume.tex
/resume-*.tex

# Legacy working dir from the old data/ model
data/

# Vendored browse: installed-on-first-run deps + setup marker (source is tracked)
vendor/browse/node_modules/
vendor/browse/bun.lock

# Scraped job artifacts (per-session working data)
jobs/
.gstack/
